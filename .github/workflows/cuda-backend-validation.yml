name: CUDA Backend Validation

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  cuda:
    runs-on: [self-hosted, linux, cuda]
    timeout-minutes: 120
    env:
      JULIA_NUM_THREADS: "1"
      OPENBLAS_NUM_THREADS: "1"
      OMP_NUM_THREADS: "1"
      MKL_NUM_THREADS: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Julia
        uses: julia-actions/setup-julia@v3
        with:
          version: "1.12"

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v3

      - name: Instantiate CUDA test project
        run: julia --project=test/cuda --startup-file=no -e 'using Pkg; Pkg.instantiate()'

      - name: Run retained CUDA hardware target
        run: julia --project=test/cuda --startup-file=no test/runtests_cuda.jl

      - name: Run focused CUDA extension coverage
        env:
          ADAPTIVEOPTICS_TEST_COVERAGE: "true"
        run: julia --project=test/cuda --startup-file=no --code-coverage=user test/cuda/extension_coverage.jl

      - name: Process CUDA coverage
        uses: julia-actions/julia-processcoverage@v1
        with:
          directories: src/backends,ext

      - name: Upload CUDA coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: lcov.info
          disable_search: true
          fail_ci_if_error: true
          name: adaptiveopticssim-cuda
          use_oidc: true
