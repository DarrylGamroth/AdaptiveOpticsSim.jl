# Model Validity Matrix

Status: active

## Purpose

This document is the maintained validation surface for major model families in
AdaptiveOpticsSim.jl.

It answers four questions explicitly:

1. what model family is being validated
2. what class of evidence exists for it
3. what the current primary external baseline is
4. what assumptions, limits, or known non-equivalences still apply

This file is the maintained matrix. Historical inventories and planning notes
are intentionally not part of the live docs set.

## Validation Classes

- `A`: analytic / structural check
  - invariants, conservation checks, deterministic replay, sanity identities,
    shape/range contracts
- `R`: frozen reference-bundle check
  - versioned committed bundles compared within tolerance
- `G`: backend parity / GPU contract
  - CPU-to-maintained-accelerator functional parity or smoke surfaces;
    currently AMDGPU and CUDA
- `P`: benchmark / runtime evidence
  - maintained runtime profiles or benchmark scripts on realistic cases
- `M`: maintained model note
  - spec, limitations doc, or explicit non-equivalence record

Validation classes are intentionally distinct:

- `A` answers “is the model internally coherent?”
- `R` answers “does it match a frozen external or committed baseline?”
- `G` answers “does the backend implementation behave consistently?”
- `P` answers “does it behave plausibly on realistic maintained workloads?”
- `M` answers “what is intentionally not claimed?”

## Detector Family Qualification Contract

Shared detector infrastructure and individual physical detector families are
qualified separately. A detector-family row must state all of the following:

- the physical effects and acquisition modes claimed by the row
- the analytic, independent numerical, or frozen external oracle for each claim
- the tolerance and its type-, scale-, or statistical rationale
- the fixed seed, sample count, and predeclared moment or distribution bounds
  for every stochastic claim
- CPU reference coverage and the applicability of each maintained accelerator
  surface
- the performance boundary measured, including whether it is service-time,
  closed-loop, open-loop, in-process, or external-transport evidence
- explicit nonclaims and known approximations

A single fixed-seed image is characterization evidence, not stochastic-physics
qualification. CPU is the reference qualification target. AMDGPU evidence is
required only for surfaces already declared maintained. WSL CUDA results are
hardware validation and do not by themselves promote a production-support
claim. Accelerator stochastic implementations are not samplewise equivalent to
CPU unless a family row explicitly proves that equivalence.

The PE-04 detector acquisition refactor changes ownership representation, not
the detector equations or qualification envelopes below. Focused
[`prepared_detector_ownership.jl`](../test/testsets/prepared_detector_ownership.jl)
evidence covers the run-immutable plan, exact prepared owner, separate
persistent state/workspace/products, deterministic workspace recreation,
failure-before-mutation behavior, and warmed CPU allocation contract. The
AMDGPU and WSL CUDA artifacts cited by detector-family rows were recorded before
that representation change. They continue to qualify the stated physical and
numerical surfaces at their recorded revisions, but do not by themselves
qualify the new owner layout; that requires a focused hardware rerun. GPU launch
allocation is not part of the CPU zero-allocation claim.

## Frozen Bundle Roots

- OOPAO and pyTomoAO bundles:
  - [test/reference_data](../test/reference_data)
  - bundle doc: committed OOPAO reference data under `test/reference_data`
- SPECULA-targeted contract bundle:
  - [test/reference_data_specula](../test/reference_data_specula)
  - bundle doc: committed SPECULA reference data under `test/reference_data_specula`

## Generation / Refresh Surfaces

- OOPAO:
  - [generate_oopao_reference_bundle.py](../scripts/generate_oopao_reference_bundle.py)
- pyTomoAO:
  - [generate_pytomoao_reference_bundle.py](../scripts/generate_pytomoao_reference_bundle.py)
- SPECULA-targeted contract bundle:
  - [generate_specula_reference_bundle.jl](../scripts/generate_specula_reference_bundle.jl)

## Matrix

| Family ID | Model family | Evidence | Primary baseline | Evidence links | Assumptions / limits | Current status |
| --- | --- | --- | --- | --- | --- | --- |
| `MV-01` | Atmosphere: finite and infinite multilayer propagation | `A`, `G`, `P`, `M` | analytic + backend parity | [atmosphere tests](../test/testsets/atmosphere.jl), [direction-batch tests](../test/testsets/atmosphere_direction_batch.jl), [prepared RNG tests](../test/testsets/plant_rng.jl), [optional backend matrix](../test/backend_optional_common.jl), [gpu_smoke_contract.jl](../scripts/gpu_smoke_contract.jl), [profile_atmosphere_runtime.jl](../scripts/profile_atmosphere_runtime.jl), atmosphere statistics artifact under `benchmarks/results/atmosphere/`, [2026-04-01-phase1-pvp02.toml](../benchmarks/results/atmosphere/2026-04-01-phase1-pvp02.toml) | Explicit elapsed/absolute time, current-state epoch-token validation, analytic wind motion, fixed-seed replay, NGS/LGS direction order invariance, frozen source inputs, and allocation-free CPU rendering are validated. A homogeneous direction-batch capability for finite and infinite multilayer models freezes ordered NGS, finite-height LGS, science, or extended-source geometry and writes one caller-owned atmospheric OPD stack. CPU slices exactly match the serial renderer and allocate zero warmed heap bytes. Accelerator geometry, pupil support, layer screens, and output remain on one concrete device with scalar indexing disabled; one three-dimensional extraction kernel is submitted per layer and completion is synchronized before return. Prepared-contract validation allocates zero host heap bytes, while backend launch descriptors have an explicit 64 KiB-per-completed-batch host-allocation regression ceiling rather than being misreported as device-workspace allocation. Incompatible model, layer/source order, grid, shape, type, backend, device, or capacity fails before destination mutation. Prepared multilayer plants require stable layer identities and derive one stateful stream per layer; named-layer stream assignment and rendered OPD remain invariant under layer reordering. The epoch token does not retain layer state after the writer advances. The batch itself also retains no historical atmosphere state. No frozen external infinite-atmosphere parity bundle exists yet, but a committed fixed-seed finite/infinite statistics artifact records variance agreement, stationarity, and non-periodicity evidence. | strong |
| `MV-02` | Phase statistics and covariance helpers | `A`, `G`, `M` | analytic | [runtests.jl](../test/runtests.jl), [gpu_smoke_contract.jl](../scripts/gpu_smoke_contract.jl), atmosphere runtime tests, phase-statistics tests and tolerances in this matrix | `K_{5/6}` helper accuracy is now summarized in a maintained note over `x ∈ [1e-6, 140]`; there is still no frozen external phase-statistics bundle | medium-strong |
| `MV-03` | Core optics: electric field, Fraunhofer, Fresnel, direct imaging, atmospheric field propagation | `A`, `R`, `G`, `P`, `M` | analytic + SPECULA-targeted contract bundle | [runtests.jl](../test/runtests.jl), [direct_science.jl](../test/testsets/direct_science.jl), [direct_imaging_batch.jl](../test/testsets/direct_imaging_batch.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [batch-submission profiler](../scripts/profile_direct_imaging_batch_submissions.jl), [reference_harness.jl](../test/reference_harness.jl), [reference_data_specula](../test/reference_data_specula), committed SPECULA reference data under `test/reference_data_specula`, [gpu_smoke_contract.jl](../scripts/gpu_smoke_contract.jl), [Gate 0 science-stage artifact](../benchmarks/results/gate0/2026-07-16-pre-hil-06-science-stage.toml), [profile_atmospheric_field_runtime.jl](../scripts/profile_atmospheric_field_runtime.jl), atmospheric-field tests and scripts | Prepared direct imaging writes a caller-owned focal-plane angular, cell-integrated photon-arrival-rate product; resolves a finite integer off-axis shift during preparation according to the declared axis order/signs; preserves same-grid incoherent asterism composition; and retains differing spectral grids as a bundle. Compatible physical native-Fraunhofer `Source` leaves may instead retain their ordered direction- and wavelength-specific products while sharing one stacked field allocation, one stacked photon-rate allocation, and one FFT plan over the optical axes. The fixed compatibility signature includes the propagation-model type, pupil grid, radiometry family, numeric type, backend family, concrete device, FFT dimensions, spectral/directional sample order, and detector-facing product contract. CPU tests cover shared and path-local pupils, single/monochromatic/spectral cases, periodic multi-field shifts, detector fan-out, whole-batch nonmutation, inference, and zero warmed heap allocation; the maintained CUDA/AMDGPU matrix additionally checks independent same-device parity, device-resident views and parameter storage, scalar-index prohibition, explicit completion, plan reuse, and bounded launch-descriptor allocation. The batch-submission profiler supplies a reproducible operation-count workload for vendor traces; it does not claim external-RTC latency. This capability does not batch Fresnel, LGS/WFS, coronagraph/PROPER, external-optics, or detector execution and does not copy, resample, combine, or regroup unsupported leaves. Placement remains nearest-sample and periodic rather than subpixel with finite-field loss. The Gate 0 artifact establishes only warmed serial CPU service-time regression bounds; it is not external-RTC latency or production-scale capacity evidence. External atmospheric-field evidence remains narrower than full platform-level numerical equivalence. | strong within the declared native-Fraunhofer batch envelope |
| `MV-04` | Shared frame-detector radiometry, response, and acquisition pipeline | `A`, `G`, `P`, `M` | independent algebra/convolution/transfer oracles + analytic moment bounds + backend parity + self-paced service cost | [shared qualification tests](../test/testsets/detector_shared_qualification.jl), [detector contract tests](../test/testsets/detector_shared.jl), [response and MTF tests](../test/testsets/detector_response.jl), [plant device model matrix](../test/testsets/plant_device_model_matrix.jl), [optional backend matrix](../test/backend_optional_common.jl), [low-fidelity service-cost artifact](../benchmarks/results/detectors/2026-08-01-shared-low-fidelity-service-cost.toml) | This row qualifies the family-neutral prepared frame boundary from declared cell-integrated or density photon-arrival rate through centered presampling response, physical-pixel integration, exact-once QE/exposure, configured binning, PRNU/bad-pixel signal effects, photon/background statistics, dark current and DSNU, sensor-specific charge effects, nonlinearity and saturation, charge transfer or pre-read gain, post-collection IPC, read noise, post-read gain/output effects, correction, quantization, background subtraction, and output conversion. Independent small-array algebra covers the applicable deterministic order; fixed 16-frame, 16,384-sample CPU tests bound Poisson shot/dark and Gaussian read-noise mean/variance at six analytic standard errors. `NullFrameResponse`, Gaussian, rectangular-aperture, sampled, and IPC realization use independent zero-extended convolution oracles; MTF checks cover DC, both axes, and a two-axis frequency. Sampled kernels are centered impulse responses, and finite-support edge loss remains distinct from pixel integration. `detector_mtf` belongs only to the configured presampling response: it excludes IPC, is not inferred from detector technology, and is neither a finite-frame nor continuous subpixel-aperture MTF. The minimal `NoiseNone`/null-response prepared path is inferred, deterministic, uses no multi-read product state, and allocates zero warmed CPU heap bytes. Its raw-HdrHistogram artifact measures one-thread, serial, self-paced in-process service time; it is not fixed-arrival, transport, external-RTC, or overload evidence. Maintained AMDGPU checks and WSL CUDA validation cover deterministic response/acquisition and IPC parity, residency, and scalar-index prohibition; stochastic accelerator samplewise equivalence is not claimed. This row deliberately does not qualify CCD, EMCCD, CMOS, HgCdTe, Skipper CCD, InGaAs, APD, SPAD, or MKID family physics. | strong for the declared shared frame pipeline and minimal deterministic path; family qualification remains separate |
| `MV-05` | Shack-Hartmann WFS | `A`, `R`, `G`, `P`, `M` | OOPAO parity + SPECULA historical characterization | [reference_harness.jl](../test/reference_harness.jl), [reference_data](../test/reference_data), [reference_data_specula](../test/reference_data_specula), committed SPECULA reference data under `test/reference_data_specula`, [wfs_stage_contracts.jl](../test/testsets/wfs_stage_contracts.jl), [plant_device_model_matrix.jl](../test/testsets/plant_device_model_matrix.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [Gate 0 Shack-Hartmann artifact](../benchmarks/results/gate0/2026-07-16-pre-hil-07-shack-hartmann-stage.toml), [profile_ao3k_runtime.jl](../scripts/profile_ao3k_runtime.jl) | OOPAO remains the primary frozen parity baseline. The narrow SPECULA polychromatic detector-frame data characterizes the retired reference-wavelength index-grid approximation through a test-only oracle adapter; it is not a supported production contract. `WavefrontSensors` is the canonical owner of the composed Shack–Hartmann family, while `Optics` independently owns the microlens model and prepared propagation. The maintained prepared path decomposes the immutable microlens model, propagation plan/workspace owner, physical front-end definition, layout/calibration, detector acquisition, estimator workspace, and caller-visible products. `ShackHartmannWFS.front_end` owns only the propagation-free geometric or diffractive physical definition; `ShackHartmannOptics` composes diffractive execution explicitly, and superseded mixed states, top-level optical fields, property forwarding, and whole-WFS optical-owner dispatch are absent. Compatible, co-resident, equal-rate prepared SH paths may now share one exact device owner/context and one atmosphere-direction batch while retaining their existing lenslet/spectral pipelines and original path-local products. The maintained CPU/CUDA/AMDGPU matrix covers off-axis NGS, finite-height LGS, monochromatic and spectral paths, exact scheduler/epoch/command/disposition state, common aberration, selected-path NCPA, native-DM state, residency, completed return, and scalar-index prohibition. Its named NGS and LGS rows additionally pass the actual device-ready WFS rate product through CCD single-read and frame-transfer EMCCD lifecycles, respectively, before the explicit synchronized host oracle. Singleton, unequal schedule/origin, mixed-family/signature/product, Zernike, and Curvature groups remain independent by explicit fallback. The current regular square microlens model uses angular focal-plane coordinates and does not yet model focal length, fill factor, per-lenslet prescriptions, or manufacturing errors. It forms cell-integrated photon-rate mosaics from explicit pupil/field inputs, preserves LGS elongation/sodium-layer profiles and same-wavelength asterisms, and retains distinct wavelength grids as separate native-sampling products in an `OpticalProductBundle`; the composed single-product `measure!` path requires one common numerical wavelength grid. Geometric and centroid signals both use `[axis 1; axis 2]` blocks with Julia column-major lenslet order; an explicit test-only oracle adapter maps those products to OOPAO's axis and NumPy storage convention. Acquired estimation accepts detector-declared real observation units, requires explicit finite calibration, and rejects a changed calibration binding before output mutation. `Asterism` remains a flat, common-wavelength leaf-source list and one acquisition requires a common optical calibration signature, so mixed NGS/LGS leaves or differing LGS geometry/profile leaves use independent WFS paths. Extended-source quadrature and rate conservation are supported by the composed convenience path, but direct WFS propagation does not yet apply component angular offsets to focal-plane morphology; direction-dependent atmosphere rendering remains path-local. The Gate 0 artifact establishes warmed serial CPU service-time regression bounds only; the device-owner matrix is correctness/residency evidence, not fused-WFS-kernel, external-RTC latency, or open-loop capacity evidence. | strong |
| `MV-06` | Pyramid and Bi-O-edge WFS | `A`, `R`, `G`, `P`, `M` | OOPAO + narrow SPECULA detector-frame contracts | [reference_harness.jl](../test/reference_harness.jl), [reference_data](../test/reference_data), [reference_data_specula](../test/reference_data_specula), committed SPECULA reference data under `test/reference_data_specula`, [wfs_stage_contracts.jl](../test/testsets/wfs_stage_contracts.jl), [plant_device_model_matrix.jl](../test/testsets/plant_device_model_matrix.jl), [plant_autonomous_optics.jl](../test/testsets/plant_autonomous_optics.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [Gate 0 Pyramid/Bi-O-edge stage artifact](../benchmarks/results/gate0/2026-07-17-pre-hil-08-pyramid-bioedge-stage.toml), [gpu_smoke_contract.jl](../scripts/gpu_smoke_contract.jl) | OOPAO remains the primary parity baseline; SPECULA anchors a narrow maintained polychromatic Pyramid detector-frame contract rather than grouped/platform equivalence. `WavefrontSensors` is the canonical owner of both composed sensor families, while `Optics` independently owns their physical phase and amplitude masks. The prepared paths retain distinct Pyramid phase-mask and Bi-O-edge amplitude-mask optics while sharing normalized focal-plane modulation quadrature and reusable propagation mechanics. Compatible, co-resident, equal-rate paths of either exact family may share one device owner/context and atmosphere-direction batch while retaining the family-specific prepared optical pipeline. The maintained CPU/CUDA/AMDGPU matrix covers NGS/LGS, spectral cases, scheduler/epoch/command/optic state, common and selected aberrations, residency, completion, and explicit incompatibility fallback; it does not group Pyramid with Bi-O-edge or promote Zernike/Curvature into this owner. Each named spectral row additionally passes its explicit first ordered device-ready spectral product through the corresponding global-shutter CMOS or HgCdTe lifecycle before the synchronized host oracle. This branch-level check does not claim that one detector has combined the complete spectral bundle. Both form normalized-pupil-coordinate, cell-integrated photon-rate mosaics from explicit pupil functions or electric fields, preserve zero/circular/user modulation, binning and pupil geometry, simple and sodium-layer-profile LGS behavior, detector response/QE/exposure ordering, calibration reference subtraction, and optical gain. The native autonomous-device implementation is currently specific to circular Pyramid modulation: bounded radius and enable changes regenerate the fixed normalized quadrature in place, while frequency, instantaneous phase, and trigger resets remain analytic device state because a complete-cycle optical average is phase invariant. Frozen CPU tests preserve the prior circular optical output exactly at the initial setpoint and cover free-running/source-relative/delivered-reset timing, branch delay/skew/jitter/phase-step/drop/duplicate behavior, safe-on-silence response, conflict rejection, and zero warmed concrete waveform-evaluation allocation. Current Gate 4 CUDA and AMDGPU hardware targets directly cover dynamic device-resident radius/enable regeneration, disabled-state centering, and normalized quadrature weights with scalar indexing disabled. Detector acquisition still applies exposure exactly once. This does not validate partial-cycle effects, runtime recalibration, a physical steering-mirror servo, or autonomous Bi-O-edge modulation; those require separate evidence. Acquired estimation accepts real floating-point or integer detector mosaics, converts samples to estimator precision before differential arithmetic, validates backend/device placement, supports geometry-preserving detector reduction, and rejects stale propagation or calibration revisions before destination mutation. Distinct spectral samples and path-local Asterism/ExtendedSource pupils remain typed bundles rather than implicit index sums. The path-local input requirement is intentional: source angular offsets are not reapplied inside the front end after atmosphere/path rendering. Mixed NGS/LGS leaves or differing LGS geometry/profile leaves therefore remain independently rendered and calibrated paths. Geometric variants are direct floating-point estimators with no diffractive workspace. The committed Gate 0 artifact records zero warmed allocation and passing predeclared absolute and relative p99 gates for staged Pyramid `G0-PERF-06`; it is serial in-process service-time evidence only, and the device-owner matrix adds correctness/residency rather than fused-kernel, external-RTC latency, or open-loop capacity evidence. | strong within the stated cycle-averaged scope |
| `MV-07` | Curvature and Zernike WFS | `A`, `R`, `G`, `P`, `M` | frozen legacy oracle + SPECULA-targeted contract bundle | [reference_harness.jl](../test/reference_harness.jl), [reference_data_specula](../test/reference_data_specula), committed SPECULA reference data under `test/reference_data_specula`, [zernike_and_curvature.jl](../test/testsets/zernike_and_curvature.jl), [wfs_stage_contracts.jl](../test/testsets/wfs_stage_contracts.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [Gate 0 Zernike/Curvature stage artifact](../benchmarks/results/gate0/2026-07-17-pre-hil-09-zernike-curvature-stage.toml), [profile_atmospheric_field_runtime.jl](../scripts/profile_atmospheric_field_runtime.jl) | `WavefrontSensors` is the canonical owner of both composed sensor families, while `Optics` independently owns `ZernikePhaseSpot` and `CurvatureDefocusPair`. The prepared paths separately own run-immutable optical and estimation plans, replaceable propagation and estimator workspaces, persistent reference calibration, caller-visible products, and exact execution bindings while preserving the legacy PupilFunction/ElectricField optical results. Curvature forms an ordered positive-/negative-defocus tuple and preserves the legacy propagation, crop, binning, and branch rate adjustment. Separate detectors may use unequal exposure and independent response/QE/readout; an explicitly packed detector requires compatible branch metadata and one shared exposure. Presampling response and its derived detector MTF remain branch-local acquisition effects after WFS optical propagation. The legacy branch throughput/background is a relay transmission/stray-light rate approximation, not detector MTF. Prepared estimators accept real floating-point or integer observations, remove explicit deterministic branch rate scales, bind calibration revisions, and remain allocation-free after warmup. Current frozen external coverage is contract-oriented and scenario-aligned to SPECULA tests; it is not complete numerical equivalence to the full SPECULA platform. Direct staged Zernike/Curvature optics accept leaf sources only; unsupported composite wrappers fail structurally. Atmosphere-aware legacy Curvature asterisms remain flat directional lists whose leaves share one optical calibration signature; heterogeneous NGS/LGS or LGS-profile paths require independent sensors. The Gate 0 artifact is warmed serial CPU service-time evidence, not external-RTC latency or open-loop capacity evidence. | strong |
| `MV-08` | LiFT and gain-sensing camera | `A`, `R`, `G`, `P`, `M` | OOPAO + maintained workflow and Gate 0 artifacts | [reference_harness.jl](../test/reference_harness.jl), [reference_data](../test/reference_data), [wfs_lift.jl](../test/testsets/wfs_lift.jl), [optical_analysis.jl](../test/testsets/optical_analysis.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [2026-04-01-phase1-pvp05.toml](../benchmarks/results/workflows/2026-04-01-phase1-pvp05.toml), [Gate 0 LiFT artifact](../benchmarks/results/gate0/2026-07-18-pre-hil-10-lift.toml), [full final Gate 0 catalog](../benchmarks/results/gate0/2026-07-18-pre-hil-11-full-backend-evidence.toml), [final Gate 0 confirmation](../benchmarks/results/gate0/2026-07-18-pre-hil-11-shack-hartmann-confirmation.toml) | `WavefrontSensors` owns the LiFT model, observation contract, estimator state, and qualified reconstruction generic. LiFT separates an immutable optical definition and single-writer forward workspace from independently acquired caller-owned observations and iterative estimation. The forward model predicts a monochromatic, angular-coordinate, cell-integrated photon-arrival rate with explicit deterministic response/sampling/binning; rate, expected-count, and normalized-intensity observations convert explicitly to the canonical rate domain. Frozen analytic/numerical Jacobian norms, coefficient recovery, weighting/damping policies, object kernels, mismatch rejection, Float32, and zero-allocation normal-equation execution are covered on CPU. The optional matrix exercises device-resident forward, dense/separable convolution, and estimation with scalar indexing disabled; the current maintained hardware pass directly completed `421/421` CUDA and `431/431` AMDGPU checks rather than inferring accelerator support from CPU tests. The GSC remains a separate focal-plane calibration workflow rather than part of LiFT acquisition. | strong |
| `MV-09` | Explicit closed-loop model composition and control primitives | `A`, `R`, `G`, `P`, `M` | OOPAO traces + maintained model profiles | [reference_harness.jl](../test/reference_harness.jl), [reference_data](../test/reference_data), [control_primitives.jl](../test/testsets/control_primitives.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [profile_ao3k_runtime.jl](../scripts/profile_ao3k_runtime.jl), [closed_loop_workload.jl](../benchmarks/support/closed_loop_workload.jl), [2026-07-13-control-operators.toml](../benchmarks/results/platform/2026-07-13-control-operators.toml) | Current evidence covers explicit atmosphere/render/sense/reconstruct/apply loops, model-specific AO188/AO3k composition, delay lines, dense/factorized/controlled reconstructor residency, caller-owned calibration storage, and retained compact OOPAO traces. The benchmark workload owns its loop explicitly and is not a generic runtime API. A factorized performance artifact using a synthetic truncated operator does not validate a production optical rank. Device-resident optical execution may still synchronize at individual kernel dependencies, so the evidence is a correctness/residency boundary rather than a claim that a complete model is one asynchronous launch chain. HIL-neutral scheduling and command timing are validated separately by the Plant rows; external-RTC pacing remains unclaimed. | strong for primitives and explicit model loops |
| `MV-10` | Tomography and reconstruction | `A`, `R`, `G`, `M` | pyTomoAO / OOPAO-adjacent frozen references | [tomography.jl](../test/tomography.jl), [reference_harness.jl](../test/reference_harness.jl), [reference_data](../test/reference_data), tomography GPU profile scripts under [scripts](../scripts), tomography benchmark artifacts under `benchmarks/results/tomography/`, [2026-04-02-phase2-psp05.toml](../benchmarks/results/tomography/2026-04-02-phase2-psp05.toml) | Frozen reference and functional coverage remain strong, but the refreshed representative benchmark review still closed as an explicit scoped defer tied to a routine-maintenance timeout budget | medium-strong |
| `MV-11` | GPU backend execution policy | `G`, `P`, `M` | backend parity | [plant_device_model_matrix.jl](../test/testsets/plant_device_model_matrix.jl), [optional_amdgpu_backends.jl](../test/optional_amdgpu_backends.jl), [optional_cuda_backends.jl](../test/optional_cuda_backends.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [backend-validation-guide.md](backend-validation-guide.md), [gpu_smoke_contract.jl](../scripts/gpu_smoke_contract.jl), [profile_ao3k_runtime.jl](../scripts/profile_ao3k_runtime.jl), platform rebaseline artifacts under `benchmarks/results/platform/`, [2026-04-02-phase3-psp10.toml](../benchmarks/results/platform/2026-04-02-phase3-psp10.toml), [2026-07-14-wsl-cuda-local-amdgpu.toml](../benchmarks/results/platform/2026-07-14-wsl-cuda-local-amdgpu.toml), [final local CPU evidence](../benchmarks/results/platform/2026-07-18-pre-hil-11-local-cpu.toml), [final WSL CPU evidence](../benchmarks/results/platform/2026-07-18-pre-hil-11-wsl-cpu.toml), [final WSL CUDA evidence](../benchmarks/results/platform/2026-07-18-pre-hil-11-wsl-cuda.toml), [Gate 7 single-device catalog](../benchmarks/results/gate7/manifest.toml) | Backend evidence is split across functional tests, maintained hardware targets, and benchmarks. The maintained CUDA and AMDGPU targets run with scalar indexing disabled and include Plant-integrated direct-science and maintained SH/Pyramid/Bi-O-edge WFS owners retaining an exact backend context, a device-resident atmosphere-direction batch, exact same-device path-product ownership, and an explicit completed return. The WFS owner deliberately reuses each family's prepared optical pipeline rather than asserting a generic fused WFS kernel. The same matrix directly exercises six conventional CCD/EMCCD/CMOS/HgCdTe response/readout combinations with device-resident state, products, and storage. CPU parity covers numerical products plus scheduler, atmosphere epoch, commands/dispositions, native-DM state, common/selected aberrations, detector transition/readout sequence, MTF, and exposure ordering. The clean July 18 artifacts preserve historical CPU parity, synchronized backend-ready, CUDA host-readout and transfer-only cost, allocations, residency, manifest identity, and run dispersion. The Gate 7 catalog adds predeclared first-use, raw-distribution, allocation, completion-boundary, exact-residency, numerical-parity, and submission-proxy evidence for the integrated paired WFS owner on current CPU, CUDA, and AMDGPU environments. These remain correctness/residency checks and synchronized host characterizations rather than platform-independent capacity claims, fixed-arrival external-RTC latency evidence, or mixed/multi-GPU placement evidence. | strong for maintained numerical and declared device-owner surfaces |
| `MV-12` | Tutorials and workflow examples | `A`, `M` | local workflow correctness | [examples/tutorials](../examples/tutorials), [reference_and_tutorials.jl](../test/testsets/reference_and_tutorials.jl), [user-guide.md](user-guide.md) | Tutorials are executed, but they are not external parity evidence by themselves | medium |
| `MV-13` | Prepared multi-path and multi-acquisition Plant composition | `A`, `G`, `P`, `M` | internal serial Plant oracle + retained historical platform artifacts | [plant_preparation.jl](../test/testsets/plant_preparation.jl), [plant_event_composition.jl](../test/testsets/plant_event_composition.jl), [plant_device_batching.jl](../test/testsets/plant_device_batching.jl), [plant_device_model_matrix.jl](../test/testsets/plant_device_model_matrix.jl), [direct_science.jl](../test/testsets/direct_science.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [Gate 2 serial plant artifact](../benchmarks/results/gate2/2026-07-21-serial-plant.toml), [Gate 3 composed artifact](../benchmarks/results/gate3/2026-07-21-multi-rate-plant.toml), retained historical grouped/platform artifacts under `benchmarks/results/` | The maintained Plant evidence covers one shared atmosphere, independent NGS/LGS/science directions, unequal acquisition schedules, one prepared rate result borrowed by several detectors, stable per-owner RNGs, and explicit product contracts. Each detector applies its own response and exposure after WFS optical propagation. The deterministic serial CPU lifecycle remains the oracle. Compatible co-resident equal-rate physical `Source` direct-science paths may execute through one prepared accelerator owner with shared atmosphere and optical batches. Compatible maintained Shack-Hartmann, Pyramid, or Bi-O-edge paths may separately share an exact device owner/context and atmosphere-direction batch while retaining their existing family pipelines and path-local results. Focused CPU and maintained hardware tests preserve numerical products, epoch/RNG/command state, common and selected optics, acquisition transition/readiness sequences, device residency, and completed return; explicit singleton, schedule/origin, family/signature/product, Zernike, and Curvature cases retain independent execution. Historical grouped-runtime artifacts outside Gate 6 characterize removed code and are not current implementation evidence; the current prepared grouped CPU ownership/parity envelope is tracked separately by `MV-30`. Auxiliary WFS measurements remain separate products rather than being implicitly concatenated into one reconstructor. This row claims no generic fused WFS kernel, mixed/multi-GPU placement, wall-clock pacing, external-RTC latency, or production-rate capacity. | strong within the declared serial and single-device owner envelope |
| `MV-14` | Independent controllable optics, DMs, and modal surfaces | `A`, `R`, `G`, `M` | deterministic CPU contracts + backend parity + narrow frozen OOPAO responses | [control_primitives.jl](../test/testsets/control_primitives.jl), [native Plant DM implementation](../src/plant/deformable_mirror.jl), [plant_command_composition.jl](../test/testsets/plant_command_composition.jl), [plant_conjugate_geometry.jl](../test/testsets/plant_conjugate_geometry.jl), [plant_mcao_moao.jl](../test/testsets/plant_mcao_moao.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [reference_harness.jl](../test/reference_harness.jl), [reference_data](../test/reference_data), [backend-validation-guide.md](backend-validation-guide.md), retained [2026-04-13 multi-optic artifact](../benchmarks/results/platform/2026-04-13-multi-optic-hil.toml) | Maintained evidence covers independent DM, tip/tilt, focus, and modal command/state/surface behavior; additive application of independently held co-conjugated optics; detector-backed invariants; and non-SH sensing surfaces. `Plant.DeformableMirrorModel` now prepares the native DM through an exact actuator schema, retains device-internal `Misregistration` separately from path/relay registration, and owns distinct active/staged runtimes with immutable shared influence data. Focused CPU evidence covers common native DMs at the pupil and two atmospheric conjugates, path-local MOAO DMs, unequal command updates, explicit multi-DM transactions, and zero warmed stage/commit/application allocation. The analytic coupling fixture independently covers NGS/LGS source footprints, registration/grid orientation, finite support, and interpolation, while the maintained AMDGPU target directly checks native device-resident state, staging storage, and surface parity with scalar indexing disabled. Frozen aggregate cases survive only as test-oracle composition of independent optics; the former aggregate optic and packed command runtime are not production APIs, and the retained historical artifact characterizes removed code rather than current performance. External evidence remains narrow: production device dynamics, calibrated instrument DMs, broad cross-simulator multi-optic equivalence, integrated GPU event-loop execution, and instrument-scale capacity remain unclaimed. | strong for native and independent optical primitives plus sampled geometric coupling; production device and instrument claims remain scoped |
| `MV-15` | Prepared acquisition product-provider semantics | `A`, `P`, `M` | internal contract and causality evidence | [plant_providers.jl](../test/testsets/plant_providers.jl), [product_providers.jl](../src/plant/product_providers.jl), [plant-and-optics.md](hil/plant-and-optics.md) | The maintained seam fixes one provider style per prepared acquisition and preserves shape, numeric type, memory domain, typed units/metadata, caller-declared geometry/radiometry metadata, and exact caller-owned result identity across compatible full-optical, user-defined reduced-order, and unchanged/copy/bounded-replay products. Core requires a metadata value and freezes it in the contract but deliberately does not prescribe an instrument metadata schema. Synthetic completed-product providers are explicitly nonresponsive and establish no optical or control claim. The schedule-free reduced-order extension fixture proves only provider dispatch, command causality, and path-execution bypass; the maintained scheduled linear reduced-order model and its narrower validity envelope are tracked separately by `MV-26`. Payload-work declarations are not latency, throughput, cache-residency, or production-shaped load evidence. WFS/detector/science fidelity remains inherited solely from the selected underlying model family and its own evidence. | strong for boundary semantics; physical fidelity is model-specific |
| `MV-16` | Prepared calibration-illumination entry semantics | `A`, `G`, `P`, `M` | internal contract + backend parity | [plant_illumination.jl](../test/testsets/plant_illumination.jl), [illumination.jl](../src/plant/illumination.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [plant-and-optics.md](hil/plant-and-optics.md) | The maintained schedule-free seam binds supported pupil-function, declared-plane field/intensity, external-optics-result, and detector-input payloads to one exact caller-owned destination, defensive metadata/backend/device contract, explicit downstream-visibility description, explicit combination semantics, epoch time, and stable path-owned RNG. CPU tests cover the native uniform intensity model and a user-defined stateful pupil evaluator, declaration-order replay, ordinary downstream Fraunhofer/detector execution, invalid contracts, and zero warmed allocation. The optional hardware matrix covers the native device-resident detector-input path with scalar indexing disabled. This establishes execution and ownership semantics only: it does not validate a physical calibration unit, relay prescription, source spectrum/coherence beyond the declared payload, HIL scheduling, trigger/setpoint control, transport, or external instrument truth. | strong for boundary semantics; source physics remains model-specific |
| `MV-17` | Schedule-free prepared serial plant composition | `A`, `P`, `M` | internal contract + clean serial CPU artifact | [plant_topology.jl](../test/testsets/plant_topology.jl), [plant_preparation.jl](../test/testsets/plant_preparation.jl), [plant_providers.jl](../test/testsets/plant_providers.jl), [plant_rng.jl](../test/testsets/plant_rng.jl), [plant_illumination.jl](../test/testsets/plant_illumination.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [Gate 2 serial plant artifact](../benchmarks/results/gate2/2026-07-21-serial-plant.toml) | The integrated CPU fixture advances one shared multilayer atmosphere to explicit absolute model time, materializes independent science, off-axis NGS Shack-Hartmann, and finite-height LGS pyramid paths, forms each selected path once, and executes four detector acquisitions; two unequal-exposure science acquisitions borrow one exact rate result. Same-seed outputs survive path/acquisition declaration reordering, warmed execution allocates zero Julia heap bytes, and three 10,000-sample raw HdrHistogram runs establish a compact self-paced service-time baseline. Provider-style and calibration-entry semantics remain covered by focused fixtures rather than being conflated with the timed workload. This does not establish event scheduling, external-RTC latency, fixed-rate capacity, production instrument scale, parallel execution, or integrated GPU plant performance; each physical model retains its own validity row. | strong for the serial ownership boundary |
| `MV-18` | Fixed-capacity deterministic plant-event scheduling | `A`, `P`, `M` | internal contract + clean serial CPU artifact | [plant_time.jl](../test/testsets/plant_time.jl), [plant_scheduler.jl](../test/testsets/plant_scheduler.jl), [scheduler implementation](../src/plant/scheduling.jl), [Gate 3 closure scheduler artifact](../benchmarks/results/gate3/2026-07-21-event-scheduler-gate3-closure.toml) | The host-side core uses checked integer-nanosecond keys, a canonical fixed-length `Memory` registry, one compact cursor per prepared generator, caller-owned fixed due slots, and one isbits claim at a time. Reversed declarations, simultaneous phases/ordinals, same-generator equal-time occurrences, activation/deactivation, long periods, stale/foreign claims, 100,000-event constant storage, inference, and zero warmed allocation are validated. The current-revision artifact contains three 20,000-sample HdrHistogram runs of unpaced scan/claim/reschedule service cost at 1, 8, 32, 128, and 256 active generators, with lossless sparse raw counts and timer overhead. The scheduler owns no detector, trigger, command, optical, queue, GPU, wall-clock, or transport semantics; the artifact is not fixed-arrival HIL latency, overload/recovery, heap-crossover, or production-scale capacity evidence. | strong for the serial calendar boundary |
| `MV-19` | Modeled trigger-source and distribution semantics | `A`, `P`, `M` | internal deterministic contract | [plant_triggers.jl](../test/testsets/plant_triggers.jl), [trigger implementation](../src/plant/triggers.jl), [time-and-scheduling.md](hil/time-and-scheduling.md) | The host-side serial core canonicalizes finite source/link/consumer fan-out independently of declaration order and maps periodic nominal edges through exact finite phase-step, jitter, label-offset, drop, and duplicate traces. Typed delivery records keep nominal, physical delivery, deterministic occurrence, fault provenance, and reported label distinct. Preparation rejects cycles, missing or unconsumed nodes, negative physical link delays, ordinary overtaking, and declarations below a conservative fixed in-flight requirement. Runtime enforces equal-time source-before-delivery causality; caller-owned delivery slots support zero warmed allocation, and 100,000 cycles retain constant prepared storage. Evidence covers exact deterministic traces and delivery semantics only: detector-specific transitions and exposure boundaries, stochastic streams, camera-internal oscillators, wall-clock pacing, rings, transport, external RTC latency, GPU execution, and instrument-scale capacity remain unclaimed. | strong for the serial exact-trace boundary |
| `MV-20` | Exact global-shutter detector-event lifecycle and scheduled HgCdTe ramps | `A`, `G`, `P`, `M` | internal detector oracle + backend parity | [plant_detector_transitions.jl](../test/testsets/plant_detector_transitions.jl), [event implementation](../src/plant/detector_acquisition_events.jl), [frame sampling](../src/detectors/frame_sampling.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [time-and-scheduling.md](hil/time-and-scheduling.md) | Preparation binds one global-shutter detector, exact typed rate-product storage, integer-nanosecond exposure/readout/readiness durations, fixed reject-on-busy behavior, and endpoint-preserving ramp read offsets. Runtime separates begin, contiguous half-open accumulation, nondestructive snapshot, close, readout completion, and readiness; predictable illegal transitions fail before detector or RNG mutation. Conventional deterministic output matches the whole-frame oracle through presampling response, QE/exposure, finite zero-extension, and charge coupling, while `detector_mtf` is unchanged. Scheduled HgCdTe reads retain evolving charge and its realized avalanche statistics across intra-exposure rate changes, then fit their actual prepared times in preallocated detector-owned products; the old direct-capture ramp remains a separately labeled synthesized convenience. Focused CPU evidence includes inference and zero warmed allocation, and the optional CUDA/AMDGPU surface keeps conventional and windowed ramp storage device resident with scalar indexing disabled. Read offsets finer than one nanosecond are rejected; interior evenly spaced offsets use floor quantization and detector read duration uses nearest-nanosecond preparation. Rolling row-band, frame-transfer, and common composition evidence is tracked separately by `MV-21`; product ports, cosmic-ray segmentation, saturation-aware ramp fitting, and correlated-noise estimation remain outside this row. | strong for the defined global-shutter boundary |
| `MV-21` | Composed multi-rate detector events | `A`, `G`, `P`, `M` | internal exact-time oracle + clean composed CPU artifact + direct lifecycle backend parity | [plant_event_composition.jl](../test/testsets/plant_event_composition.jl), [composition implementation](../src/plant/event_composition.jl), [rolling/frame-transfer implementation](../src/plant/rolling_frame_transfer_events.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [Gate 3 composed artifact](../benchmarks/results/gate3/2026-07-21-multi-rate-plant.toml), [time-and-scheduling.md](hil/time-and-scheduling.md) | The serial virtual-time fixture composes one shared multilayer atmosphere, independent science/NGS/LGS path periods, periodic and delivered-trigger starts, global CMOS, CCD, rolling CMOS, triggered HgCdTe up-the-ramp, and frame-transfer EMCCD acquisitions. Exact sequence/readiness timestamps, same-time causal phases, shared science-path fan-out, declaration-order replay, a common-source drop/phase-step/jitter/duplicate fan-out, fixed prepared storage through 25 simulated seconds, and a million-fold long-period direct jump are validated. Rolling exposure and global-reset row-band results follow exact constant-rate oracles; frame transfer uses one nonaliasing backend-resident storage frame and permits only capacity-safe image/readout overlap; detector MTF is unchanged. Warmed direct rolling and frame-transfer detector kernels allocate zero CPU heap bytes. The clean composed artifact records three 10,000-sample raw HdrHistogram runs and 987.408 observed Julia heap bytes per processed timestamp against the declared 2 KiB heterogeneous-orchestration ceiling. Optional GPU checks cover the direct rolling and frame-transfer lifecycles with scalar indexing disabled, not an integrated device-resident event loop. Complete-product sequence/readiness state is not a lease, port publication, progressive transport, wall-clock pacing, fixed-arrival latency, parallel-placement, or production instrument-scale claim. | strong for deterministic serial composition; HIL pacing and capacity remain unclaimed |
| `MV-22` | Bounded standalone core plant-command admission | `A`, `M` | internal deterministic contract | [plant_command_schemas.jl](../test/testsets/plant_command_schemas.jl), [plant_command_admission.jl](../test/testsets/plant_command_admission.jl), [command admission implementation](../src/plant/command_admission.jl), [time-and-scheduling.md](hil/time-and-scheduling.md) | One prepared endpoint binds an exact versioned schema to fixed active capacity, endpoint-owned host scalar or backend-selected array payload slots, one transactional array staging slot, a fixed accepted-sequence window, stable ordinal, and a flat sorted future calendar. Focused CPU tests cover copied/clipped payload ownership, duplicate/stale/reordered/skipped classification, future/late/capacity decisions, retry after nonadmission, absolute supersession with one credit per displaced presentation, no-backdating, deterministic scheduled-time/endpoint-ordinal/local-sequence order, opaque application-ready claim lifecycle and metadata-tamper rejection, bounded pending failure drain, foreign/stale ownership failures, constant storage across 1,000 cycles, inference, and zero warmed heap allocation for scalar and array basic cycles plus multi-entry insertion/removal, supersession, capacity rejection, and failure drain. Incremental supersession is rejected because discarding a pending delta is not equivalent. Effective-command state and silence evidence are tracked separately by `MV-23`; composed physical/event behavior is tracked by `MV-24`. This row establishes no fixed-arrival overload, HIL ring/lease, wall-clock, transport, or production instrument-scale claim. | strong for the standalone CPU admission contract |
| `MV-23` | Standalone effective-command application and plant-time silence | `A`, `G`, `M` | internal deterministic contract + direct backend residency | [plant_command_application.jl](../test/testsets/plant_command_application.jl), [command application implementation](../src/plant/command_application.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [time-and-scheduling.md](hil/time-and-scheduling.md) | One qualified single-writer application state binds an explicit initial/effective command, preallocated array staging, and any required copied safe command to an exact prepared endpoint-state owner. Focused CPU tests cover absolute replacement, incremental addition, application-stage reject/clip/fail and nonfinite results, exact scheduled application with terminal failure rather than backdating after a missed timestamp, unchanged state on rejection/failure, first-command baselines, admission/application silence origins, one-shot rearming, exact equal-time command precedence, safe-state ownership, terminal fail with bounded pending drain, cross-owner rejection, inference, and zero warmed heap allocation. Direct optional CUDA/AMDGPU checks run with scalar indexing disabled and retain effective, staging, payload, and safe arrays on the selected device. This validates standalone command-state and replay semantics; composed physical publication is tracked by `MV-24`. Device-specific slew/settling/hysteresis, command-to-surface calibration, fixed-arrival HIL latency, and production instrument capacity remain unclaimed. | strong for the standalone application contract |
| `MV-24` | Composed virtual-time command and co-conjugated-optic execution | `A`, `M` | internal exact-time and optical-response contract | [plant_command_composition.jl](../test/testsets/plant_command_composition.jl), [plant_placed_optics.jl](../test/testsets/plant_placed_optics.jl), [plant_conjugate_geometry.jl](../test/testsets/plant_conjugate_geometry.jl), [plant_mcao_moao.jl](../test/testsets/plant_mcao_moao.jl), [event composition implementation](../src/plant/event_composition.jl), [native Plant DM implementation](../src/plant/deformable_mirror.jl), [controllable-optic extension seam](../src/plant/controllable_optics.jl), [pupil-footprint coupling](../src/plant/pupil_footprint_coupling.jl), [Gate 4 command-responsive plant artifact](../benchmarks/results/gate4/2026-07-24-command-plant.toml), [time-and-scheduling.md](hil/time-and-scheduling.md), [plant-and-optics.md](hil/plant-and-optics.md) | `prepare_plant` binds every endpoint configuration to one independently owned physical optic and resolves required optical placement plus all-path or selected-path visibility into bounded canonical bindings and co-placed groups. Native DMs retain separate active/staged command and surface storage while sharing only prepared influence data. Full-optical event preparation binds one immutable path-local coupling per visible optic; compatible contiguous couplings form execution groups without sharing command state. The serial event loop stages effective-command and physical response together, gives commands at `t` precedence over optical samples at `t`, preserves half-open exposure integration ending at `t`, and applies only each due path's visible surfaces in canonical order. An explicit transaction across distinct physical optics still preflights and stages every member before all-or-none publication; equal timestamps and optical grouping do not imply atomicity. Focused CPU tests cover command causality, independent rates and held state, command-during-exposure splitting, transaction success/abort/conflict credit, fail-closed configuration, initial/safe ownership, declaration/configuration reordering, selected-path isolation, prepared group invariance, analytic altitude-conjugate geometry, common native DMs at three planes over NGS/LGS/science paths, isolated target-local MOAO surfaces, zero warmed native stage/commit/application allocation, zero warmed binding access, and the declared 4 KiB command-timestamp allocation ceiling. The clean self-paced Gate 4 artifact retains independent two-optic service-cost, terminal-accounting, fixed-storage, and allocation evidence; its HdrHistograms characterize serial service cost only. Production device dynamics, calibrated instrument DMs, integrated GPU event-loop execution, fixed-arrival HIL latency, and instrument-scale capacity remain unclaimed. | strong for native serial composition, exact path visibility, and sampled geometric coupling; device and HIL evidence remain scoped |
| `MV-25` | Prepared controller-output routing | `A`, `G`, `M` | internal ownership and compatibility contract + direct backend residency | [plant_controller_routing.jl](../test/testsets/plant_controller_routing.jl), [controller routing implementation](../src/plant/controller_routing.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [plant-and-optics.md](hil/plant-and-optics.md) | Every named controller product binds to one distinct prepared endpoint, independent of declaration order. Array products may be views into a larger caller-owned buffer and remain zero-copy; scalar products use `Ref`. Preparation validates exact element type, dimensions, semantic backend, and compute device, while generated endpoint lookup and payload access allocate zero warmed CPU heap bytes. Routing deliberately adds no command sequence, effective timestamp, admission, transaction, placement, queue, transport, or optical grouping. CPU evidence is complete. Gate 4 candidate revision `191751f` directly validates a device-resident view and host/backend rejection on the maintained `421/421` CUDA and `431/431` AMDGPU targets with scalar indexing disabled. | strong for CPU and maintained accelerator preparation/access contracts |
| `MV-26` | Deterministic linear reduced-order AO direct-measurement plant | `A`, `P`, `M` | internal analytic and exact-time closed-loop contract | [reduced-order implementation](../src/plant/reduced_order.jl), [direct-measurement lifecycle](../src/plant/direct_measurement_events.jl), [event composition](../src/plant/event_composition.jl), [plant_reduced_order.jl](../test/testsets/plant_reduced_order.jl), [plant_mcao_moao.jl](../test/testsets/plant_mcao_moao.jl), [plant-and-optics.md](hil/plant-and-optics.md) | A prepared model freezes a deterministic time-correlated harmonic modal disturbance, path projection, exact endpoint-schema command-response operators, sensor operator, calibration revision, measurement/residual semantics, declared operating envelope, and explicit omitted effects. Event preparation derives the expected response slots from the optical path's direct prepared visibility range, so missing visible and extra hidden endpoint responses fail before execution. The serial event loop evaluates the residual from currently effective commands, exposure-averages held direct `WFSMeasurement` samples over exact half-open intervals, preserves periodic or delivered-trigger start plus readout/readiness timing, and bypasses unused full-optical path execution. Focused CPU evidence covers exact replay, later-product command causality, clipped-effective-command visibility, a modeled dropped trigger, independent endpoint timing, dimensions/units/sign/basis/revision rejection, matched reference loop closure, expected wrong-sign/delay/stale/drop/response-gain degradation, common-plus-target-local MOAO response isolation, both visibility failure directions, and a 2 KiB warmed allocation ceiling per processed timestamp. This is a small linear modal direct-measurement envelope, not a claim for raw pixels, detector physics, tomography fidelity, external RTC integration, integrated GPU event-loop execution, equivalence to a full-optical model, control stability outside the tested controller/scenario, or instrument-scale latency/capacity. | strong inside the declared linear direct-measurement envelope |
| `MV-27` | Native sampled pupil-OPD attachment | `A`, `G`, `M` | internal optical-order/ownership contract + direct backend residency | [plant_sampled_aberrations.jl](../test/testsets/plant_sampled_aberrations.jl), [sampled-aberration implementation](../src/plant/sampled_aberrations.jl), [sampled-aberration declarations](../src/plant/definitions.jl), [backend_optional_common.jl](../test/backend_optional_common.jl), [plant-and-optics.md](hil/plant-and-optics.md) | One immutable declaration binds a native `NCPA` or `OPDMap` to exact metric pupil-plane metadata, pupil or atmospheric-conjugate placement, all-path or selected-path visibility, additive or replacement application, and optional pupil-relay registration. Preparation makes an exact-target defensive OPD copy from supported caller input and one finite-support coupling per visible path. A path permits at most one replacement, applied before canonical placement/identity-ordered additives. Schedule-free and serial event execution order atmosphere materialization, sampled aberrations, controllable surfaces, autonomous path optics, and then the typed native or external executor. Focused CPU evidence covers common-versus-science-only isolation, unrelated-path isolation, frozen caller ownership, exact structured rejection, finite-support add/replace numerics, external-executor residual handoff, event ordering with a native DM, and zero warmed low-level plus selected-execution allocation. The maintained optional targets pass `438/438` CUDA and `448/448` AMDGPU checks with scalar indexing disabled, including exact storage residency and identity/transformed replacement. This row does not claim a dynamic NCPA estimator/controller, detailed instrument prescription, asynchronous external-optics scheduling, integrated GPU event loop, fixed-arrival latency, or instrument-scale capacity. | strong for native sampled static OPD inside the declared plane/path envelope |
| `MV-28` | Integrated conjugated multi-path optical-placement plant | `A`, `G`, `P`, `M` | internal analytic/order oracle + clean serial CPU artifact + direct backend residency | [plant_gate5_closure.jl](../test/testsets/plant_gate5_closure.jl), [Gate 5 benchmark](../benchmarks/benchmark_gate5_optical_placement.jl), [versioned contract](../benchmarks/contracts/gate5_optical_placement.toml), [clean artifact](../benchmarks/results/gate5/2026-07-25-optical-placement.toml), [backend_optional_common.jl](../test/backend_optional_common.jl) | The representative serial plant composes four alternating finite-height-LGS/NGS WFS paths and four science paths, common native DMs at the pupil and two atmospheric conjugates, one selected-path MOAO DM per science path, a common replacement OPD, science-only NCPA, explicit relay magnification, Fraunhofer propagation, and one open detector consumer per path. Analytic constant-OPD sums, exact WFS/science isolation, declaration-order replay of pupil and propagated products, explicit finite-support loss, deterministic product hashes, fixed prepared storage, and exact 4/8/16-path binding counts pass. The clean single-thread artifact observes 5,632 Julia heap bytes per warmed eight-path cycle against an 8 KiB ceiling; three 10,000-sample self-paced runs record 107.711 microseconds median p50, 124.287 microseconds worst p99, and 9.229 kcycles/s median throughput. Maintained CUDA/AMDGPU targets validate the constituent device-resident native-DM and sampled-surface operations with scalar indexing disabled, not the integrated event loop. This row makes no fixed-arrival, external-RTC, parallel-worker, integrated-GPU, detailed relay/coronagraph, production-instrument-fidelity, or NFIRAOS/MORFEO capacity claim; the topology matrix characterizes preparation growth but does not bound generated code. | strong for the declared serial CPU optical-placement envelope; accelerator composition and instrument capacity remain scoped |
| `MV-29` | Prepared whole-plant topology specialization envelope | `A`, `P`, `M` | internal type/inference contract + clean fresh-process CPU growth artifact | [definitions](../src/plant/definitions.jl), [preparation and schedule-free execution](../src/plant/preparation.jl), [owner RNG preparation](../src/plant/randomness.jl), [event composition](../src/plant/event_composition.jl), [topology-growth tests](../test/testsets/plant_topology_growth.jl), [versioned contract](../benchmarks/contracts/gate6_topology_growth.toml), [benchmark](../benchmarks/benchmark_gate6_topology_growth.jl), [clean artifact](../benchmarks/results/gate6/2026-07-25-topology-growth.toml), [execution specification](hil/execution-and-placement.md) | Whole-plant definitions, prepared owners, command endpoints, optics, aberrations, paths, acquisitions, RNG groups, schedule-free selections, and event execution groups use fixed-size homogeneous registries whose types do not encode owner count. Concrete small path, acquisition, materialization, and optical-application pipelines remain behind per-owner function barriers. Focused tests prove identical prepared definition, plant, RNG, and selection types at 4/8/16 paths plus inferred representative numerical kernels. The clean one-thread artifact independently probes event and schedule-free-selection first use in 18 fresh Julia processes over synthetic 4/8/16-path Gate 5-derived shapes: every checked registry/entry type has one fingerprint, event and selection native-code sizes have a 1.0 growth ratio, method-instance growth is zero, representative stage kernels have zero meaningful `Any` slots, median preparation time grows 1.052×, preparation allocation 1.029×, and prepared storage 2.744×. Maximum first event and selection invocation are 2.998 s and 2.234 s; warmed serial orchestration remains at most 704.04 heap bytes per path per cycle while numerical stage kernels retain their stricter allocation contracts. The HdrHistogram distributions are self-paced diagnostics. The `nfiraos_like` and `morfeo_like` labels specify cardinality only; this row makes no physical-model fidelity, fixed-arrival, external-RTC, integrated-GPU, parallel-capacity, or production-instrument claim. | strong for the declared CPU topology-representation and code-growth envelope |
| `MV-30` | Prepared grouped CPU path execution envelope | `A`, `P`, `M` | internal exact-replay/ownership contract + clean paired CPU artifact | [event composition](../src/plant/event_composition.jl), [CPU execution budgets](../src/plant/cpu_execution.jl), [group lifecycle and replay tests](../test/testsets/plant_event_composition.jl), [budget tests](../test/testsets/plant_cpu_execution.jl), [versioned contract](../benchmarks/contracts/gate6_grouped_cpu.toml), [benchmark](../benchmarks/benchmark_gate6_grouped_cpu.jl), [clean artifact](../benchmarks/results/gate6/2026-07-25-grouped-cpu.toml), [Gate 6 evidence catalog](../benchmarks/results/gate6/manifest.toml), [execution specification](hil/execution-and-placement.md) | Every canonically prepared due path has one single-writer execution group with direct begin, exact-once materialization, seal, execution, and completion seams. The selected atmosphere policy holds the writer until all due inputs are materialized; compact epoch identity is never retained-state storage. A maintained four-thread harness reuses one coordinator and three fixed path-owner tasks, perturbs materialization/execution submission order over unequal-rate science/NGS/LGS paths, demonstrates overlap on distinct threads, and exactly matches serial scheduler state, atmosphere sequence, path/acquisition products, and RNG streams. Explicit budgets reject Julia/FFT/BLAS oversubscription and preserve a one-thread deterministic fallback. The clean artifact records fresh first use, direct-call and integrated warmed allocation, GC, raw HdrHistograms, dispersion, and paired serial/grouped closed-loop service cost. Its mixed laptop result is not a general speedup claim. Test tasks and `Channel` synchronization are scaffolding only; production workers, affinity, SPSC completion, pacing, fixed-arrival external-RTC latency, integrated GPU execution, and instrument capacity remain unclaimed. | strong for the declared prepared CPU ownership, parity, and service-cost envelope |
| `MV-31` | Prepared single-device direction/WFS path-batch execution envelope | `A`, `G`, `P`, `M` | internal exact-state and residency contract + clean paired CPU/CUDA/AMDGPU artifacts | [device-batch implementation](../src/plant/device_batching.jl), [device-owner tests](../test/testsets/plant_device_batching.jl), [model matrix](../test/testsets/plant_device_model_matrix.jl), [optional hardware matrix](../test/backend_optional_common.jl), [versioned contract](../benchmarks/contracts/gate7_single_gpu.toml), [benchmark](../benchmarks/benchmark_gate7_single_gpu.jl), [artifact catalog](../benchmarks/results/gate7/manifest.toml), [execution specification](hil/execution-and-placement.md) | Compatible co-resident equal-rate direct-science, Shack-Hartmann, Pyramid, or Bi-O-edge paths bind to one run-immutable accelerator owner retaining one concrete compute device, backend context, atmosphere-direction batch, model-specific optical pipelines, original path-product handoffs, and an explicit completion boundary. Incompatible, singleton, CPU, unsupported-family, unequal-schedule, mixed-product, and cross-device groups remain independent. The declared conventional detector/readout matrix is validated separately from the timed owner boundary. Focused CPU and maintained hardware matrices establish deterministic CPU-oracle parity, exact discrete state, scalar-index prohibition, recursive device residency, completion, and allocation for the promoted model combinations. The clean Julia 1.12.6 artifacts characterize one 128-sample, two-layer, two-off-axis-NGS diffractive Shack-Hartmann workload with 50 warmups and three 200-operation raw-HdrHistogram runs per boundary. The prepared owner reduces the atmosphere-render submission proxy from two calls to one while retaining two WFS optics executions; batched-to-independent median p95 is 1.050 on gfx1030 AMDGPU and 0.911 on WSL RTX 3050 Ti CUDA against the 1.5 ceiling. Device-ready, host-ready, and transfer-only costs remain separate. This is synchronized self-paced single-device service-cost evidence, not fixed-arrival external-RTC latency, overload behavior, fused WFS kernels, mixed placement, multi-GPU execution, or NFIRAOS/MORFEO capacity. | strong for the declared single-device ownership, parity, residency, and service-cost envelope |
| `MV-32` | Conventional CCD single-read detector | `A`, `G`, `P`, `M` | shared-pipeline algebra + analytic moment bounds + lifecycle/backend parity + immutable family artifact | [CCD qualification tests](../test/testsets/detector_ccd.jl), [shared frame qualification](../test/testsets/detector_shared_qualification.jl), [Plant detector lifecycle](../test/testsets/plant_detector_transitions.jl), [device model matrix](../test/testsets/plant_device_model_matrix.jl), [optional backend matrix](../test/backend_optional_common.jl), [CCD family artifact](../benchmarks/results/detectors/2026-07-30-ccd-single-read-qualification.toml) | `CCDSensor` with `SingleRead` adds one independent Poisson clock-induced-charge realization per pixel per completed frame to the qualified shared frame pipeline. CIC is parameterized in electrons per pixel per frame and is invariant to exposure duration; it is not a per-second rate. Fixed 16-frame, 16,384-sample CPU contracts bound isolated shot, dark, CIC, and Gaussian read-noise moments plus their combined mean/variance at six analytic standard errors. Exact tests cover the applicable QE/binning/PRNU/full-well/gain/quantization order, configured-response MTF inheritance, per-owner replay independent of execution order, failure before output mutation, inference, and zero warmed allocation. Single-read `read_time` is rejected rather than silently treated as serial-register timing: Plant acquisition definitions independently own readout and readiness durations. Existing maintained AMDGPU and WSL CUDA matrices cover deterministic single-read CCD response/lifecycle residency with scalar indexing disabled; stochastic samplewise cross-backend equivalence is not claimed. This row excludes Skipper sampling, CTI, serial/parallel register geometry, blooming, transfer smear, radiation damage, and calibrated camera profiles. | strong inside the declared conventional single-read approximation |
| `MV-33` | EMCCD linear, photon-counting, and frame-transfer modes | `A`, `G`, `P`, `M` | exact path/saturation/timing oracles + analytic moments and CDF bounds + lifecycle/backend parity + immutable family artifact | [EMCCD qualification tests](../test/testsets/detector_emccd.jl), [frame-transfer lifecycle tests](../test/testsets/plant_event_composition.jl), [device model matrix](../test/testsets/plant_device_model_matrix.jl), [optional backend matrix](../test/backend_optional_common.jl), [EMCCD family artifact](../benchmarks/results/detectors/2026-07-30-emccd-qualification.toml) | `EMCCDSensor` distinguishes conventional and electron-multiplying outputs, linear and binary photon-counting operation, and sequential versus frame-transfer acquisition timing. The CPU-only `ConditionalGammaMultiplication` contract is tested over exponential, integer-shape Erlang, and fractional-shape cases using six-standard-error mean/variance bounds and selected analytic CDF probabilities. `ClippedGaussianMultiplicationApproximation` is a separately named, nonnegative, backend-portable moderate/high-charge approximation; it is not claimed to reproduce the conditional-Gamma distribution or low-charge event statistics. Construction rejects the conditional-Gamma policy on accelerators rather than silently changing algorithms. CIC is independent Poisson charge per completed frame and enters before multiplication. Exact tests combine input and register-referred full-well limits, enforce `em_gain_range`, verify conventional-output bypass, and preserve configured response/MTF across acquisition policies. Photon-counting evidence covers three incident-flux cases against the binary coincidence law \(\eta(1-e^{-\lambda})\); this mode is not photon-number resolving within one frame. Frame transfer changes first-frame latency and overlapped steady-state period, not optical response; scheduled Plant lifecycle evidence retains one nonaliasing storage frame and independent readout timing. The CPU conditional-Gamma prepared path is inferred and allocates zero warmed heap bytes. Maintained AMDGPU and WSL CUDA checks cover nonnegativity and the mean of the portable approximation plus deterministic frame-transfer lifecycle/residency with scalar indexing disabled; they do not establish accelerator variance fidelity, and CUDA validation is not a production-support claim. This row excludes frame-transfer smear, CTI, stage-by-stage register physics, register aging, calibrated gain laws, and camera profiles. | strong inside the declared per-frame model envelopes; approximation-specific accelerator claim |
| `MV-34` | Parameterized CMOS-family detector | `A`, `G`, `P`, `M` | exact shutter/pipeline oracles + analytic spatial-covariance bounds + lifecycle/backend parity + immutable family artifact | [CMOS qualification tests](../test/testsets/detector_cmos.jl), [rolling lifecycle tests](../test/testsets/plant_event_composition.jl), [device model matrix](../test/testsets/plant_device_model_matrix.jl), [optional backend matrix](../test/backend_optional_common.jl), [CMOS family artifact](../benchmarks/results/detectors/2026-07-30-cmos-family-qualification.toml) | One `CMOSSensor` architecture covers ordinary CMOS, scientific CMOS, and quantitative low-noise CMOS through explicit parameters rather than camera types or profiles. Global-shutter, rolling-exposure, and global-reset row-band timing have exact constant, stepped, pulsed, and interval-source oracles; the scheduled Plant lifecycle uses exact integer timestamps. A `FrameWindow` selects output from a completed full frame and therefore does not shorten timing; a physical ROI is represented by cropped detector geometry. Fixed 16,384-sample CPU contracts bound mean, variance, same-column covariance, same-row covariance, and independent-pixel/diagonal covariance at six analytic standard errors for isolated and combined Gaussian components. Exact algebra covers PRNU, DSNU, bad pixels, charge-domain saturation, detector gain, static output-group gain/offset, quantization, output conversion, and configured-response MTF inheritance. Construction rejects nonfinite noise, output-pattern, and timing parameters. The warmed rolling direct-capture path allocates zero CPU heap bytes. Maintained AMDGPU and WSL CUDA checks cover global/rolling deterministic behavior, structured readout effects, device residency, and scalar-index prohibition; stochastic samplewise cross-backend identity is not claimed. The qualified envelope assumes frame-independent Gaussian row, column, and per-pixel components. It excludes temporal 1/f noise, random-telegraph noise, calibrated maps and camera profiles, on-camera filtering, and vendor-specific readout modes. | strong inside the declared independent-noise and row-band timing envelope |
| `MV-35` | Conventional HgCdTe nondestructive-read detector | `A`, `G`, `P`, `M` | exact sampling/ramp/timing oracles + analytic moment bounds + lifecycle/backend parity + immutable family artifact | [HgCdTe qualification tests](../test/testsets/detector_hgcdte.jl), [Plant detector lifecycle](../test/testsets/plant_detector_transitions.jl), [device model matrix](../test/testsets/plant_device_model_matrix.jl), [optional backend matrix](../test/backend_optional_common.jl), [HgCdTe family artifact](../benchmarks/results/detectors/2026-07-30-conventional-hgcdte-qualification.toml) | `HgCdTeSensor` represents a conventional area array without avalanche multiplication; `HgCdTeAvalancheArraySensor` is a distinct linear-avalanche architecture. Both compose the immutable `Detectors.HgCdTeReadout` configuration. Single read, averaged nondestructive reads, correlated double sampling, Fowler sampling, and up-the-ramp sampling have exact no-noise and analytic independent-Gaussian read-noise contracts. Fixed 16,384-sample CPU cases bound the read-noise variance of all five modes and the mean/variance of dark-current and readout-glow Poisson charge at six analytic standard errors. Direct `capture!` labels its lower-fidelity evenly spaced product `:synthesized_final_charge`; scheduled Plant acquisition labels `:scheduled_evolving_charge`, records every actual integer-timestamp read, and fits those possibly irregular times. Exact tests cover slope/intercept, retained cube/timestamps, full-frame/window timing, reference correction, saturation, nonlinearity, persistence, configured response/MTF, post-collection IPC, deterministic replay, inference, and zero warmed CPU heap allocation. A `FrameWindow` selects the output of a completed full-frame acquisition and does not shorten read time. No HgCdTe technology name implies optical blur, MTF, or IPC; those remain explicit models. Maintained AMDGPU and WSL CUDA checks cover device-resident deterministic sampling products and scheduled ramps with scalar indexing disabled; stochastic samplewise cross-backend equivalence is not claimed. The envelope assumes linear accumulation and independent per-read Gaussian noise. It excludes cosmic-ray jump detection or ramp segmentation, saturation-aware fitting, correlated 1/f noise, reciprocity failure, calibrated persistence or detector maps, signal-dependent IPC, vendor/camera profiles, and qualification of avalanche multiplication. The retained raw cube and actual timestamps permit richer fitting outside this model. | strong inside the declared conventional clean-ramp envelope |
| `MV-36` | HgCdTe linear-avalanche photodiode array | `A`, `G`, `P`, `M` | exact ordering/saturation/readout oracles + analytic conditional moments + lifecycle/backend residency + immutable family artifact | [linear-avalanche qualification tests](../test/testsets/detector_hgcdte_avalanche.jl), [Plant detector lifecycle](../test/testsets/plant_detector_transitions.jl), [optional backend matrix](../test/backend_optional_common.jl), [linear-avalanche family artifact](../benchmarks/results/detectors/2026-07-31-hgcdte-linear-avalanche-qualification.toml) | `HgCdTeAvalancheArraySensor` is a linear-avalanche area detector, not a Geiger-mode SPAD or photon-timestamp stream. Its excess-noise factor uses the detector-literature definition \(F=\mathrm{E}[M^2]/\mathrm{E}[M]^2\), giving conditional output variance \(g^2(F-1)q\) for input charge \(q\) and mean avalanche gain \(g\). `Detectors.ConditionalGammaAvalancheMultiplication` is a nonnegative CPU reference model with shape \(q/(F-1)\) and input-referred scale \(F-1\). Fixed 16-frame, 16,384-sample cases cover single-carrier, multiple-carrier, and low-excess-noise shapes against predeclared six-standard-error mean/variance bounds. `Detectors.ClippedGaussianAvalancheMultiplicationApproximation` is separately named and backend portable; its statistical claim is restricted to \(q/(F-1)\geq25\), where clipping is negligible, and no low-charge distribution fidelity is claimed. Construction rejects the conditional-Gamma policy on accelerators rather than changing algorithms silently. Exact tests prove avalanche gain before read noise and conversion gain, dark current and readout glow before multiplication, input-referred saturation, full-frame/window timing, all shared HgCdTe sampling modes, explicit response/MTF, post-collection IPC, deterministic replay, and separation from counting detectors. Scheduled up-the-ramp execution applies multiplication only to newly accumulated charge, retaining earlier realizations in later reads. CPU conditional-Gamma, portable approximate direct capture, and scheduled approximate ramps are inferred where applicable and allocate zero warmed heap bytes. Maintained AMDGPU and WSL CUDA checks cover the portable moderate-charge distributional moments, nonnegativity, direct residency, conditional-Gamma rejection, and scheduled device-resident realization retention; stochastic samplewise CPU/GPU equality is not claimed. The row excludes bias-voltage and gain-temperature calibration, per-pixel gain maps, bandwidth-dependent electronics, signal-dependent IPC, vendor/camera profiles, Geiger-mode counting, and photon timestamps. | strong inside the CPU conditional-Gamma and moderate-charge portable-approximation envelopes |
| `MV-37` | Skipper CCD bounded independent-read sampling | `A`, `G`, `P`, `M` | exact retained-charge/storage/timing oracles + analytic variance-versus-sample-count bounds + backend residency + immutable family artifact | [Skipper qualification tests](../test/testsets/detector_skipper.jl), [optional backend matrix](../test/backend_optional_common.jl), [Skipper family artifact](../benchmarks/results/detectors/2026-07-31-skipper-ccd-qualification.toml) | `SkipperSampling(n)` repeatedly measures one retained CCD charge packet and forms an online arithmetic mean. It stores one mean frame and a fixed sample count, never an \(n\)-plane raw cube. `CCDSensor.sample_duration` is the elapsed duration of one configured full-frame nondestructive sample; it is neither a sampling period nor an electronics integration-window model. Exact cases cover charge retention, input-referred full-well saturation, output gain, fixed storage, metadata, batched-capture rejection before mutation, deterministic replay, inference, and zero warmed CPU allocation. Fixed 16,384-value cases at \(n=1,4,16,64\) bound zero mean and variance \(\sigma_1^2/n\) at six analytic standard errors. Maintained AMDGPU and WSL CUDA checks cover one `n=16` variance-and-residency witness, the device-resident mean product, fixed storage, timing, and scalar-index prohibition; the CPU-only `n=1,4,64` points and stochastic samplewise equality are not accelerator claims. The row excludes correlated 1/f noise, optimal filtering, dual-slope-integration electronics, adaptive stopping, region-dependent schedules, charge-transfer inefficiency, and camera profiles. | strong inside the fixed-count independent-Gaussian-read envelope |
| `MV-38` | Product-neutral InGaAs frame detector | `A`, `G`, `P`, `M` | exact null-response/pipeline/persistence oracles + analytic Poisson moment bounds + backend residency + immutable family artifact | [InGaAs qualification tests](../test/testsets/detector_ingaas.jl), [shared frame qualification](../test/testsets/detector_shared_qualification.jl), [thermal detector models](../test/testsets/detector_thermal.jl), [optional backend matrix](../test/backend_optional_common.jl), [InGaAs family artifact](../benchmarks/results/detectors/2026-07-31-ingaas-qualification.toml) | `InGaAsSensor` composes the strong shared frame pipeline without inferring a material-specific blur, pixel aperture, MTF, IPC, spectral QE, or camera profile. Glow is an independent Poisson charge rate applied over integration duration, not a read-cadence glow law; ordinary dark current, defects, QE, nonlinearity, saturation, gain, and explicit response remain shared `Detector` configuration. `ExponentialPersistence(coupling, decay)` is a deterministic per-completed-frame reduced model with charge-domain recurrence \(l_{k+1}=\mathrm{decay}\,l_k+\mathrm{coupling}\,q_k\). For InGaAs, \(q_k\) is after nonlinearity, saturation, and charge coupling and before read noise, conversion gain, correction, quantization, and background subtraction; exact tests prove that latent state is independent of conversion gain. The coefficients are dimensionless and do not scale with elapsed time. Fixed 16,384-value CPU cases bound combined glow and dark-current Poisson mean/variance at six analytic standard errors. Exact cases cover default and explicit response, MTF non-assumption, deterministic pipeline ordering, three-frame illuminated/dark persistence recurrence, incremental equivalence, deterministic replay, batched-persistence rejection before mutation, inference, and zero warmed CPU allocation. The WSL CUDA target on Julia 1.12.6 passes device-resident recurrence, null response, gain independence, glow/dark moments, scalar-index prohibition, and the full maintained 1,014-check CUDA matrix. The local AMDGPU 2.7.0 target passes the 12-check deterministic InGaAs recurrence, response, gain-independence, and residency gate; its stochastic Poisson gate is not promoted here because GPUCompiler segfaulted while compiling that kernel on the maintained Julia 1.12.6 host. Stochastic samplewise equality is not claimed on either accelerator. The envelope excludes calibrated wavelength response, trap populations or time constants, exposure-dependent persistence, afterglow spectra, read-cadence-dependent glow, correlated 1/f or burst noise, universal InGaAs MTF, and vendor/camera profiles. | strong for the explicit shared-frame effects and discrete charge-domain persistence envelope; not a calibrated InGaAs material or camera model |
| `MV-39` | Product-neutral linear-mode APD channels | `A`, `G`, `P`, `M` | exact topology/order oracles + analytic shot/multiplication/read-noise moment bounds + prepared Curvature channel mapping + immutable family artifact | [linear-mode APD qualification tests](../test/testsets/detector_linear_apd.jl), [prepared WFS stage contracts](../test/testsets/wfs_stage_contracts.jl), [optional backend matrix](../test/backend_optional_common.jl), [linear-mode APD family artifact](../benchmarks/results/detectors/2026-07-31-linear-apd-qualification.toml) | `LinearAPDDetector` models one analog element or a fixed bank of independent channels with one-dimensional storage; it is not an area frame. The removed generic `APDDetector` no longer overlaps Geiger-mode `SPADArrayDetector`. The deterministic order is photon flux times QE and integration duration, plus integrated dark current, optional Poisson statistics, linear avalanche multiplication, additive read noise, then conversion gain. The avalanche excess-noise factor uses the detector convention \(F=\mathrm{E}[M^2]/\mathrm{E}[M]^2\); the portable approximation adds conditional input-referred variance \((F-1)q\) and is qualified only for \(q/(F-1)\geq25\), where clipping is negligible. Fixed 16,384-channel CPU cases bound multiplied shot noise, multiplication-only noise, and read noise at six analytic standard errors. Exact cases cover scalar and fixed-bank input, topology metadata, signal ordering, predictable shape and parameter failures, deterministic replay, inference, and zero warmed CPU allocation. Prepared Curvature acquisition maps its two branch values per pupil sample into a correctly sized linear-APD bank without presenting a two-dimensional detector API and reuses its bound storage without warmed CPU allocation. The maintained accelerator matrix covers deterministic vector gain/order and residency only; stochastic CPU/accelerator equality and an accelerator distributional claim are not made. No detector MTF applies because this API declares channels rather than pixels. The envelope excludes Geiger operation, photon timestamps, bandwidth-dependent electronics, transimpedance-amplifier response, correlated channel noise, calibrated gain-bandwidth products, and named modules or camera profiles. | strong inside the declared independent-channel and moderate-charge approximation envelope |
| `MV-40` | Product-neutral SPAD accumulated-count array | `A`, `G`, `P`, `M` | exact radiometry/dead-time/redistribution oracles + analytic Poisson-surrogate moment bounds + prepared WFS/backend residency + immutable family artifact | [SPAD qualification tests](../test/testsets/detector_spad.jl), [prepared WFS stage contracts](../test/testsets/wfs_stage_contracts.jl), [optional backend matrix](../test/backend_optional_common.jl), [SPAD family artifact](../benchmarks/results/detectors/2026-08-01-spad-qualification.toml) | `SPADArrayDetector((rows, columns); ...)` models a fixed-shape Geiger-mode accumulated-count area detector over already cell-integrated photon-arrival-rate input. The deterministic order is active-area detection efficiency, fill factor, live integration time, and source throughput; plus integrated dark counts; then a nonparalyzable or paralyzable dead-time mean law and explicitly selected deterministic mean-response stages. `FirstOrderAfterpulseMeanResponse` scales expectation by one plus the mean first-order afterpulse count per detection. `NearestNeighborCountRedistribution` is a count-conserving reduced image operation, not an electrical-crosstalk claim. `NoisePhoton()` samples a Poisson distribution only after those mean adjustments. Fixed 16,384-cell CPU cases bound photon-only, dark-only, combined, and dead-time-adjusted surrogate moments at six analytic standard errors. Exact CPU curves cover \(\lambda\tau\) from zero through 100, the nonparalyzable \(1/\tau\) asymptote, and the paralyzable \(1/(e\tau)\) peak and high-rate collapse. Further checks cover active-area efficiency times fill factor, gated live time, edge/corner redistribution, fixed dimensions and preallocated storage, finite nonnegative input, integer conversion, metadata, replay, inference, and zero warmed allocation. Prepared WFS acquisition binds dimensions/backend/device once and uses the prevalidated device-resident path. Maintained CUDA evidence covers direct residency, deterministic dead-time/redistribution/integer results, a dead-time-adjusted Poisson moment case, and scalar-index prohibition. The maintained AMDGPU target retains the deterministic residency and numerical checks; its stochastic SPAD gate is not promoted because GPUCompiler segfaulted while compiling the portable Poisson kernel on the Julia 1.12.6 host. This remains an accumulated expected-count/Poisson surrogate: it excludes photon-event timestamps, avalanche waveforms, exact dead-time and afterpulse count distributions, history-dependent afterpulsing, spectral efficiency curves, calibrated maps, named detector modules, and detector MTF. Fill factor is scalar radiometry and does not infer a pixel aperture. | strong for fixed-array accumulated-count means and the explicitly bounded Poisson surrogate; event-resolved SPAD statistics remain outside the model |
| `MV-41` | Product-neutral MKID accumulated-count array | `A`, `G`, `P`, `M` | exact radiometry/passband/dead-time oracles + analytic Poisson-surrogate moment bounds + prepared WFS contract + immutable family artifact | [MKID qualification tests](../test/testsets/detector_mkid.jl), [prepared WFS stage contracts](../test/testsets/wfs_stage_contracts.jl), [optional backend matrix](../test/backend_optional_common.jl), [MKID family artifact](../benchmarks/results/detectors/2026-08-01-mkid-qualification.toml) | `MKIDArrayDetector` consumes already cell-integrated photon-arrival rate and produces one accumulated expected-count or sampled-count image. The deterministic order is quantum efficiency, scalar fill factor, live integration time, and source throughput; plus integrated dark counts; followed by a nonparalyzable or paralyzable dead-time mean law, optional Poisson sampling, and output conversion. Exact cases cover QE/fill/gate/dark algebra, inclusive passband boundaries, weighted spectral-bundle throughput, matrix-only prefiltered capture, integer rounding/saturation, optional-characteristic metadata, finite nonnegative failure before mutation, deterministic replay, inference, and zero warmed CPU allocation. Fixed 16,384-cell CPU cases bound photon-only, dark-only, combined, and dead-time-adjusted Poisson-surrogate moments at six analytic standard errors. Exact CPU curves cover \(\lambda\tau\) from zero through 100. `MKIDArrayCharacteristics` separates optional energy resolving power, photon-arrival-time resolution, and wavelength passband from the declared `:accumulated_count_image` observable. Prepared ordinary and packed-Curvature WFS plans require a source only when a passband is configured, compute source throughput once, and reuse their bound device-resident path. Maintained AMDGPU and WSL CUDA checks cover deterministic direct parity, passband-boundary throughput, residency, and scalar-index prohibition; they do not establish an accelerator stochastic-distribution claim, and CUDA validation is not a production-support claim. This model does not include SPAD afterpulse or count-redistribution stages. It excludes per-photon energy estimates, photon-arrival timestamps, measured energy distributions, pulse pile-up, resonator dynamics, quasiparticle physics, readout-tone multiplexing, calibrated spectral-efficiency curves, detector maps, and camera profiles. No detector spatial response or MTF is modeled or inferred: inputs are cell-integrated per detector pixel, and scalar fill factor does not define a pixel aperture or presampling response. | medium-strong for the accumulated-count proxy and its declared scalar characteristics; not an event-resolved, calibrated, or spatial-response MKID model |
| `MV-42` | Preparation-only mixed CPU/accelerator plant partitioning | `A`, `G`, `M` | exact topology, ownership, residency, RNG-identity, and structural-byte oracles | [assignment tests](../test/testsets/plant_partition_assignments.jl), [target-partition tests](../test/testsets/plant_target_partitions.jl), [optional backend matrix](../test/backend_optional_common.jl), [partition assignment implementation](../src/plant/partition_assignments.jl), [partition preparation implementation](../src/plant/partitions.jl), [execution and placement specification](hil/execution-and-placement.md) | One cold `PlantDefinition` may be prepared as CPU-only, one-accelerator-only, or CPU-plus-one-accelerator target-local resources after the caller supplies a complete exact assignment. Canonical stable path identities determine placement; acquisitions are inseparable from their paths. Topology-sized mappings and prepared collections use homogeneous registries whose container types do not encode path or endpoint count; focused two-versus-three-path checks defend the type fingerprint. One `PreparedAtmosphereAuthority` owns the sole timed atmosphere, mutable timeline, and atmosphere RNG state, while every non-executable target partition retains the same immutable identity binding. Target-local telescope data, visible static sampled aberrations, complete path/acquisition resources, reusable workspaces, role-neutral physical-optic owners, and endpoint active/staging effective values have exact target-scoped facts; an unprepared atmosphere renderer, lifecycle, scheduler, command publisher, payload lease, or runtime transfer is not counted. Cold preparation may defensively copy declared static data and configured initial effective commands into every exact target that uses them. Fixed CPU/fake-accelerator tests prove canonical ordering, complete co-location, sole atmosphere and command identities, exact byte totals, static-OPD and command residency, no path-count double counting, placement-invariant versioned RNG metadata, and structured missing/duplicate/foreign/stale/unavailable/multiple-accelerator failures. WSL CUDA hardware validation on Julia 1.12.6 and CUDA.jl 6.2.1 additionally proves accelerator-only and mixed preparation, exact residency, deterministic local DM application, and reports without transferring products between domains. The corresponding AMDGPU optional checks exist, but this public target-partition path is not promoted on AMDGPU: on the local `gfx1030` device with Julia 1.12.6 and AMDGPU.jl 2.7.0, normal compilation terminates the containing process; manual staged construction and `--compile=min` complete. [Issue #216](https://github.com/DarrylGamroth/AdaptiveOpticsSim.jl/issues/216) tracks that no-command reproducer separately from command-validation issue #213. A queued GitHub AMDGPU run with no eligible self-hosted runner is infrastructure state and supplies no validation evidence. This row does not claim atmosphere or authoritative command publication, payload transfer, cross-domain synchronization, a mixed executor, HIL placement/admission, capacity, latency, numerical CPU/GPU equality, multi-GPU, multi-process, or runtime migration. | strong for the preparation-only caller-resolved topology and exact-resource envelope on CPU/fake targets and WSL CUDA; AMDGPU and mixed execution remain unqualified |
| `MV-43` | Versioned target-local effective-command publications | `A`, `G`, `M` | exact identity/schema/order/alias/staging/residency/resource oracles + warmed allocation contract | [publication tests](../test/testsets/plant_command_publications.jl), [target-partition tests](../test/testsets/plant_target_partitions.jl), [optional backend matrix](../test/backend_optional_common.jl), [publication implementation](../src/plant/command_publications.jl), [execution and placement specification](hil/execution-and-placement.md) | Each prepared partition set creates one unforgeable placement-neutral `CommandAuthorityIdentity`. Every exact target prepares at most one role-neutral owner for each visible logical optic, shared by every co-located path, with independent copied active/staging effective values per endpoint and one shared physical state/workspace. Immutable `EffectiveCommandPublication` metadata carries the authority identity, exact target, optic, versioned schema identity and declared numeric/physical metadata, authoritative plant timestamp, and positive endpoint-local sequence without owning mutable array payload storage. Local staging accepts only already target-resident finite effective values with exact type, shape, units, sign, basis, and schema revision; it never reapplies incremental-command semantics. Sequence must increase, timestamp must not regress, and one optic-wide staging owner prevents different endpoints from overwriting a shared physical candidate. Every declared failure is rejected before active effective values, physical state, or committed publication metadata change. Fixed CPU tests prove caller/staging/endpoint/owner alias isolation, equal-time sequence ties, unequal-rate hold decisions, declaration-order invariance, scalar and array behavior, shared-workspace exclusion, exact resource bytes, concrete cold-resolved endpoint bindings, inference, and zero warmed local stage/commit heap allocation. Optional WSL CUDA checks prove exact device residency and deterministic local DM application with scalar indexing disabled. The AMDGPU check is present but remains unpromoted because GPUCompiler terminates while compiling finite-array command validation even with `--compile=min`; [issue #213](https://github.com/DarrylGamroth/AdaptiveOpticsSim.jl/issues/213) tracks that path separately from the no-command partition failure in [issue #216](https://github.com/DarrylGamroth/AdaptiveOpticsSim.jl/issues/216). Public mutation is intentionally withheld: the HIL mixed-composition gate still owns sole-publisher binding, bounded payload slots and leases, transfer submission/completion, fan-out, pacing, and outcome accounting. | strong for role-neutral CPU/fake-target and WSL CUDA local publication/application semantics; AMDGPU, cross-domain publication, and mixed execution remain unqualified |
| `MV-44` | Typed bounded cross-device handoff state machine | `A`, `G`, `M` | exact type/axes/device/capacity/alias/provenance/generation/state oracles + explicit fake transfer + warmed allocation contract | [handoff tests](../test/testsets/plant_handoffs.jl), [handoff implementation](../src/plant/handoffs.jl), [backend transfer seam](../src/backends/arrays.jl), [execution and placement specification](hil/execution-and-placement.md) | One `PreparedCrossDomainHandoff` binds a concrete immutable semantic payload contract, paired fixed-capacity source/destination slots, distinct exact host/accelerator devices, exact isbits element type and axes, logical payload byte count, prepared source/destination contexts, and an opaque identity tied to the exact prepared partition set. One external owner serializes every lifecycle method; the value is not thread-safe. The caller exclusively owns a selected free source before submission; successful submission transfers it to the handoff. Destination storage is handoff-owned after preparation and becomes consumable only after a separate nonblocking completion observation succeeds. Generation-bearing references enforce nominal `free → submitted → completed | failed → reclaimed` lifetime and reject foreign, invalid, stale, duplicate, incomplete, and exhausted-generation transitions without changing valid accounting. A backend may report failure only after guaranteeing quiescence; an unexpected exception or unsupported status leaves the slot uncertain and non-reclaimable. CPU↔fake-accelerator tests use test-local concrete arrays with scalar indexing disabled, contexts, and explicit transfer methods to prove bidirectional copy semantics, context restoration, pending/completed/quiescent-failed/uncertain observations, terminal-only reclamation, typed publication visibility, explicit absence of a generic transfer fallback, inference, and zero warmed lifecycle heap allocation. Core provides no generic `copyto!`, polling loop, queue, wait strategy, payload lease, scheduling, backpressure, or transport. This evidence excludes real CUDA/AMDGPU transfer behavior, peer GPU transfer, hardware context/event performance, mixed optical execution, HIL fanout/planning, throughput, and latency. | strong for the bounded ownership and transfer-state semantics on CPU/fake storage; hardware transfer and integrated HIL execution remain unqualified |

| `MV-45` | Authority-owned path-specific pupil-OPD publication | `A`, `G`, `M` | exact route/epoch/lifecycle/retention oracles + CPU↔fake-accelerator numerical parity + warmed allocation contract | [publication tests](../test/testsets/plant_path_input_publications.jl), [publication implementation](../src/plant/path_input_publications.jl), [handoff tests](../test/testsets/plant_handoffs.jl), [execution specification](hil/execution-and-placement.md) | One explicitly prepared route binds the exact partition set, sole atmosphere authority, target-local path, frozen source direction, native `PupilFunction`, and authoritative epoch/timestamp. A same-target route materializes atmospheric OPD directly. A remote route owns one fixed typed handoff slot, materializes into authority-local storage, observes completion separately, and then copies only OPD into the target-local pupil. Support, amplitude, static aberrations, controllable-optic surfaces, propagation workspaces, and path results are not transferred. Generation-bearing metadata and route state reject wrong opaque route identity, atmosphere, epoch, timestamp, path, slot, generation, stale reuse, premature apply, and invalid lifecycle transitions before target-local mutation. The native materialization factory closes direct construction and binds the exact authority telescope, requested frozen source, path pupil, and source-specific renderer; adversarial tests reject both a renderer prepared for another direction and a same-grid, same-revision telescope with a different aperture. The opaque route identity binds that exact prepared materialization without retaining the source or resource graph in publication metadata. CPU↔fake-accelerator tests with scalar indexing disabled prove two distinct source directions at one shared atmosphere epoch, direct/remote equality with serial `PreparedPathExecutor` oracles, retained-publication independence from later atmosphere mutation, quiescent transfer failure, fail-stop uncertain ownership, unchanged target-local support/amplitude, concrete inline publication storage, inference, and zero warmed lifecycle heap allocation. Partition preparation remains route-free unless the caller explicitly prepares one. This evidence excludes the cross-route all-due barrier, integrated mixed execution, static/controllable-optic composition, real CUDA/AMDGPU transfers, multi-accelerator execution, throughput, and latency. | strong for the declared one-route ownership, metadata, retention, and fake-target numerical envelope; integrated and hardware mixed execution remain unqualified |

The final
[detector qualification closure catalog](../benchmarks/results/detectors/2026-08-01-detector-qualification-closure.toml)
is common exact-revision evidence for `MV-04` and `MV-32` through `MV-41`.
Candidate `dd16596` passed 3,192 bounded CPU qualification/composition checks
with one intentional broken marker, 244/244 focused local AMDGPU detector
checks, 250/250 focused WSL CUDA detector checks, and the 1,028/1,028 complete
WSL CUDA target. The AMDGPU InGaAs and SPAD stochastic Poisson gates remain
explicit exclusions, and CUDA remains hardware-validation evidence rather than
a production-support claim. Historical backend counts below remain tied to
their named revisions and do not replace this detector-specific catalog.

The July 18 backend pass strengthens `MV-03` through `MV-09` and `MV-11`: the
full CPU suite passed, the maintained CUDA target passed `424/424`, and the
maintained AMDGPU target passed `434/434`. It predates the controller-routing
delta in `MV-25` and is not cited as hardware evidence for that row. Clean
REVOLT-like correctness,
residency, allocation, and serial service-time evidence is retained for
[local CPU](../benchmarks/results/platform/2026-07-18-pre-hil-11-local-cpu.toml),
[WSL CPU](../benchmarks/results/platform/2026-07-18-pre-hil-11-wsl-cpu.toml),
and [WSL CUDA](../benchmarks/results/platform/2026-07-18-pre-hil-11-wsl-cuda.toml).
The composed [Gate 0 catalog](../benchmarks/results/gate0/2026-07-18-pre-hil-11-full-backend-evidence.toml)
and [same-contract confirmation](../benchmarks/results/gate0/2026-07-18-pre-hil-11-shack-hartmann-confirmation.toml)
preserve both the observed 96 ns relative-p99 miss and the immediate passing
repeat. No new AMD latency baseline is claimed after the host Julia
installation failed before retaining a raw histogram.

The Gate 4 candidate revision `191751f` adds current hardware evidence without
rewriting those latency baselines: the maintained CUDA target passed `421/421`
on an RTX 3050 Ti under WSL and the AMDGPU target passed `431/431` on local
gfx1030, both on Julia 1.12.6 with scalar indexing disabled. This directly
closes the prepared controller-routing and dynamic cycle-averaged
circular-Pyramid surfaces in `MV-06`, `MV-11`, and `MV-25`. The clean
[Gate 4 command-responsive plant artifact](../benchmarks/results/gate4/2026-07-24-command-plant.toml)
strengthens `MV-24` as serial self-paced CPU service-cost evidence; it does not
claim fixed-arrival HIL latency or integrated GPU scheduling.

Gate 5 closure validation head `02e5f29` passed `448/448` checks on the same
local gfx1030 AMDGPU target and `438/438` on the WSL RTX 3050 Ti CUDA target,
both with Julia 1.12.6 and scalar indexing disabled. CUDA used CUDA.jl 6.2.1;
both used KernelAbstractions.jl 0.9.42. The matrix retains native Plant DM
state/staging/surface parity and adds exact device-resident defensive
sampled-OPD ownership plus identity and transformed replacement. This extends
the accelerator evidence in `MV-11`, `MV-14`, `MV-24`, and `MV-27`; it does
not validate the integrated event loop on a GPU.

The clean
[Gate 5 optical-placement artifact](../benchmarks/results/gate5/2026-07-25-optical-placement.toml)
adds the integrated serial CPU evidence in `MV-28`. Its exact numerical,
declaration-order, finite-support, fixed-storage, bounded-allocation, and
4/8/16-path binding-count gates all pass. Preparation growth is recorded
rather than hidden; generated-code bounds, parallel placement, integrated GPU
execution, fixed arrivals, and production instrument capacity remain later
gates.

Across `MV-03` through `MV-09` and `MV-13`, the telescope owns aperture
geometry and reflectivity but no cadence or mutable wavefront product. Each
optical path owns an explicit `PupilFunction`, and controllable surfaces are
formed independently before being applied to a selected path. Explicit model
loops pass atmosphere elapsed time directly; the Plant event loop derives
model time from canonical timestamps. The WFS optics stage produces a rate
independent of both that interval and detector exposure. Focused rate,
unequal-exposure, density-versus-cell-integrated, compatible-sum, typed-bundle,
and prepared WFS-stage tests provide CPU and representative CUDA/AMDGPU
residency evidence for the ownership boundary. The direct-science stage has
focused CPU and maintained CUDA/AMDGPU evidence for caller-owned image formation and
detector fan-out. The serial event loop adds independent multi-rate virtual-time
composition, but neither surface establishes wall-clock HIL scheduling or
external-RTC timing evidence. Model-specific loops may deliberately own one
sequential RNG; the prepared Plant instead derives exact per-owner streams
whose results are invariant to path, acquisition, selection, and named
atmosphere-layer order. This is serial scenario-replay evidence, not yet
addressable multi-device RNG evidence. Stage fixtures establish composition
semantics, not another WFS physics model or a fidelity promotion. Composed WFS
objects may retain prepared optical, acquisition, and estimation workspaces,
but those stages have explicit products and single-writer ownership.

Elementwise spectral or source accumulation is valid only for compatible
physical grids with declared incoherent-addition semantics. Incompatible
prepared products remain in an `OpticalProductBundle` or require an explicit
prepared mapping; array shape alone is not evidence of compatibility.

For the HgCdTe surfaces qualified separately from the family-neutral `MV-04`
pipeline, up-the-ramp sampling currently fits a linear ramp with independent
read noise and retains the read cube and timestamps. It shares the
exposure-level photon/dark realization across reads and does not yet model
cosmic-ray segmentation, saturation-aware fitting, or correlated 1/f noise.
EMCCD frame transfer is an acquisition-timing model: it changes reported
one-frame latency and steady-state period, but not the optical or charge
response.

## Known Baseline Rules

- OOPAO is the primary frozen parity baseline for:
  - PSF
  - Shack-Hartmann
  - Pyramid
  - Bi-O-edge
  - LiFT
  - gain-sensing camera
  - compact closed-loop traces
- pyTomoAO remains the frozen baseline for tomography model/reconstructor data.
- SPECULA is the targeted external baseline where it is the stronger reference
  for behavior breadth rather than legacy parity, currently captured through the
  committed contract bundle for:
  - atmospheric field propagation
  - Zernike WFS
  - Curvature WFS

## Known Non-Equivalences and Scope Limits

- Infinite-atmosphere fidelity is not currently claimed against a frozen OOPAO
  or SPECULA statistics bundle.
- SPECULA-targeted contract bundles are intentionally narrower than full
  platform equivalence:
  - they freeze deterministic contract scenarios
  - they do not claim identical internal representation or orchestration
- Tutorial execution is evidence that workflows stay runnable, not evidence of
  scientific parity by itself.

## Acceptance Rule

For a model family to be treated as maintained and strong, the preferred target
is:

- `A` plus `R` or an explicitly justified absence of `R`
- `G` where accelerator support is claimed
- `P` where runtime/HIL or realistic execution claims are made
- `M` whenever equivalence is intentionally partial or scoped

Families that do not yet meet that shape should remain explicitly marked
medium/partial instead of being implied complete.
